services:
  osrm:
    image: ghcr.io/project-osrm/osrm-backend:latest
    container_name: osrm-taiwan-validation
    command: osrm-routed --algorithm mld /data/osrm/taiwan.osrm
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data:ro
    restart: unless-stopped
